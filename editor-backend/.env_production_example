
# =============================================================================
# UNIFIED EDITOR - PRODUCTION ENVIRONMENT VARIABLES
# =============================================================================

# =============================================================================
# DATABASE CONFIGURATION (REQUIRED)
# =============================================================================
# Used by backend to connect to PostgreSQL production database

POSTGRES_HOST=
POSTGRES_PORT=5432
POSTGRES_USER=incubrix_admin
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_SSL_MODE=require

# Optional schema if used
# POSTGRES_SCHEMA=editor_prod

# Full connection string (preferred by Go service)
# Format:
# postgres://USER:PASSWORD@HOST:PORT/DB?sslmode=require
DATABASE_URL=


# =============================================================================
# AWS S3 CONFIGURATION (REQUIRED for production storage)
# =============================================================================
# Used when STORAGE_TYPE=s3

AWS_REGION=ap-southeast-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET=
CLOUDFRONT_DOMAIN=

# Storage type selection
# Options: local | s3
STORAGE_TYPE=s3


# =============================================================================
# LOCAL STORAGE CONFIGURATION (OPTIONAL fallback)
# =============================================================================
# Used only if STORAGE_TYPE=local

UPLOAD_DIR=/var/www/uploads/editor
BASE_URL=

# File upload limits (must match validation)
MAX_FILE_SIZE=524288000


# =============================================================================
# API SERVER CONFIGURATION (REQUIRED)
# =============================================================================

PORT=8083
API_BASE_URL= api/v1

# CMS backend integration (Repurposer / Content Hub)
CMS_BACKEND_URL=

# Environment mode
APP_ENV=production
ENV=production
LOG_LEVEL=info


# =============================================================================
# CORS CONFIGURATION (REQUIRED)
# =============================================================================

ALLOWED_ORIGINS=


# =============================================================================
# AUTHENTICATION CONFIGURATION (REQUIRED for production)
# =============================================================================
# Used by API gateway or JWT validation

JWT_SECRET=
SESSION_SECRET=


# =============================================================================
# FRONTEND CONFIGURATION (editor-ui/.env.production)
# =============================================================================

VITE_DEV_CONTENT_ID=11111111-1111-1111-1111-111111111111
VITE_API_URL=http://localhost:8083/api/v1
VITE_ENV=production
VITE_MAX_FILE_SIZE=524288000


# =============================================================================
# FEATURE FLAGS (OPTIONAL)
# =============================================================================

ENABLE_AUTOSAVE=true
AUTOSAVE_INTERVAL=10000


# =============================================================================
# MONITORING & LOGGING (OPTIONAL)
# =============================================================================

# SENTRY_DSN=
# LOG_FILE=/var/log/editor/app.log
